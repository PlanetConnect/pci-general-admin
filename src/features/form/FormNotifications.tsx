import Box from "@mui/system/Box";

const FormNotifications = () => {
  return (
    <Box sx={{ maxWidth: 650 }}>
      {/* <Form size="xl" validationSchema={EmailSchema} onSubmit={onSubmit}>
        <Header>Notification Form</Header>
        <Section name="basic info">
          <DateField value="2022-03-10" name="endDate" label="End Date" />
          <Switch label="Is Active?" name="isActive" isChecked={true} />
          <TextField type="text" label="Email" name="email" />
          <TextField type="password" label="Password" name="password" />
          <Select label="Status" name="status" options={status} />
        </Section>

        <Section name="address">
          <AddressField address={{}} />
        </Section>

        <Actions>
          <SaveButton />
        </Actions>
      </Form> */}
    </Box>
  );
};

export default FormNotifications;
